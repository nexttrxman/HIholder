-- ============================================================
-- Migración: 000_template (renombra el archivo: 001_mi_cambio.sql)
-- Fecha: YYYY-MM-DD
-- Aplicada en: [ ] Supabase   [ ] vps/postgres/init/01-schema.sql
-- ============================================================

-- Escribe aquí el cambio (CREATE TABLE, ALTER TABLE, funciones, índices...).
-- Debe ser idempotente cuando sea posible (IF NOT EXISTS / OR REPLACE),
-- para poder re-ejecutarla sin romper nada.

-- EJEMPLO:
-- ALTER TABLE users ADD COLUMN IF NOT EXISTS language TEXT DEFAULT 'es';
